/**
 * Enum for OPERATIONS indicator values .
 * @readonly
 * @enum {number}
 */
const OPERATION = {
    /** 1 indicates that it is INSERT operation */
    INSERT: 1,
    /** 2 indicates that it is UPDATE operation */
    UPDATE: 2,
    /** 3 indicates that it is DELETE operation */
    DELETE: 3
};

module.exports = OPERATION;